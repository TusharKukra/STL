#include<iostream>
#include<list> //header file for List
using namespace std;

// Forward List: it keeps internally only the link to the next element (not like List / Double Linked List, which keeps two links per elements)
// Fwd List are more efficient bcoz they will take less memory

// read more on : http://www.cplusplus.com/reference/forward_list/forward_list/

